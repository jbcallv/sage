apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: exec-monitor
spec:
  kprobes:
    - call: "sys_execve"
      syscall: true
      args:
        - index: 0
          type: "string"
          label: "filename"
      selectors:
        - matchActions:
            - action: Post
